#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../._concurrently@2.2.0@concurrently/src/main.js" "$@"
  ret=$?
else 
  node  "$basedir/../._concurrently@2.2.0@concurrently/src/main.js" "$@"
  ret=$?
fi
exit $ret
